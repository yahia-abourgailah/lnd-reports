[alembic]
script_location = alembic
prepend_sys_path = src
path_separator = os

# The URL comes from DATABASE_URL at runtime (see alembic/env.py). Migrations
# connect as the owner role, not as the least-privilege application role, since
# DDL needs privileges the application deliberately does not have.
sqlalchemy.url =

# Migrations are named 0001_..., 0002_... so `ls versions/` reads in order.
file_template = %%(rev)s_%%(slug)s

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
