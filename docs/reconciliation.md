# Reconciliation statement — platform against workbook

<!-- GENERATED by `python -m lnd.reference.reconcile`. Do not edit by hand:
     every figure and every explanation comes from the metric registry, and a
     hand-edit would drift from the code it is supposed to describe. -->

Generated 2026-09-07 from the metric registry against the frozen reference dataset.

## The rule this is written under

The training delivered did not change. Only the arithmetic describing it did.

Every difference below has one of three causes, and it is worth naming which before showing anyone a number:

- **Coverage** — the workbook covers February to August 2026. The platform holds July 2025 to September 2026. Most of the apparent growth is this.
- **Grain** — the workbook counted the wrong thing: titles instead of program ids, a `#` column instead of a session id.
- **A defect** — the figure was wrong on its own terms.

A figure that rose because of coverage is not an improvement in performance, and a figure that fell because a defect was corrected is not a decline. Both will be read that way unless said out loud first.

## The comparison window

The **Platform** column below is computed over **1 February to 31 August 2026** — the period the workbook covers. That is the only like-for-like comparison, and it is what should be presented.

The full dataset is shown separately at the end. Comparing *that* against the workbook's column would attribute to correction what is mostly coverage.

### Unchanged definition

The platform computes these the way the workbook did. Where the number still differs it is coverage, not method: the workbook counted 40 of the 50 sessions delivered in this window, so it was measuring the same thing over less of it. Showing these first establishes that the platform agrees with the workbook wherever the workbook was right.

| Metric | Workbook | Platform | n |
|---|---|---|---|
| Total Participants | 128 | **147** | 437 |
| Learner Hours | 1,386.0 hours | **1,153.0 hours** | 437 |
| Facilitator Performance | 100.0% | **98.3%** | 118 |


**Total Participants** — Distinct people with at least one attendance in scope.

*Counts over:* one person attending one session.
*Excludes:* records soft-deleted at source by the nightly reconcile; rows whose person could not be identified — quarantined, never dropped.
*Workbook figure from BRD v1.5.*

**Learner Hours** — Hours consumed by learners: the session duration, summed once per attendance.

*Counts over:* one person attending one session.
*Excludes:* records soft-deleted at source by the nightly reconcile; rows whose person could not be identified — quarantined, never dropped.
*Workbook figure from BRD v1.5.*

**Facilitator Performance** — Share of responses rating this facilitator 4 or better on the 1-5 scale, over every response in scope that answered the question.

*Counts over:* survey responses carrying a rating for the dimension asked about.
*Excludes:* records soft-deleted at source by the nightly reconcile; rows whose person could not be identified — quarantined, never dropped; responses that left this question blank.
*Workbook figure from BRD v1.5.*

### Restated

Same intent, computed properly. The number moves because the old one was assembled by hand or keyed on the wrong thing — not because anything about the training changed.

| Metric | Workbook | Platform | n |
|---|---|---|---|
| Training Days | 40 | **50** | 50 |
| Training Hours Delivered | 130.5 hours | **135.6 hours** | 50 |
| Total Programs | 24 | **27** | 27 |
| Public vs Customised | — | **81.5%** | 27 |


**Training Days** — Distinct delivered sessions. One session is one training day.

*Counts over:* sessions belonging to completed programs.
*Excludes:* records soft-deleted at source by the nightly reconcile; sessions of programs that are not complete.
*Why it changed:* Counted on the CRM session id. The workbook counted its own '#' column, which was a row counter as well as an identifier, so a distinct count over it meant nothing (P-12).
*Workbook figure from BRD v1.5.*

**Training Hours Delivered** — Sum of session durations. Hours delivered once, not once per attendee.

*Counts over:* sessions belonging to completed programs.
*Excludes:* records soft-deleted at source by the nightly reconcile; sessions of programs that are not complete.
*Why it changed:* Distinct from Learner Hours, which multiplies by attendance. A two-hour session with thirty people is 2 hours delivered and 60 learner hours; the workbook's single 'hours' column was read as both.
*Workbook figure from BRD v1.5.*

**Total Programs** — Distinct programs the CRM marks completed.

*Counts over:* programs the CRM marks completed.
*Excludes:* records soft-deleted at source by the nightly reconcile; programs still upcoming or in progress, by computed_status not status.
*Why it changed:* Counted on computed_status, not status. The two disagree on 55 of 57 programs: off status the answer is 2, and it is 55. The workbook reported 25 because it knew only about programs somebody had pasted in.
*Workbook figure from BRD v1.5 — the week-4 handover records 25 — worth settling with L&D.*

**Public vs Customised** — Share of completed programs open to the whole company rather than to one department.

*Counts over:* programs the CRM marks completed.
*Excludes:* records soft-deleted at source by the nightly reconcile; programs still upcoming or in progress, by computed_status not status.
*Why it changed:* Read from the CRM's target field, pending L&D confirming it matches their Public Calendar / Customised split. 50 public and 7 department today — an observation, not yet a definition.

### Renamed

Same formula, a name that says what it measures.

| Metric | Workbook | Platform | n |
|---|---|---|---|
| L&D-delivered % | — | **96.3%** | 27 |


**L&D-delivered %** — Share of completed programs the CRM marks as delivered by L&D.

*Counts over:* programs the CRM marks completed.
*Excludes:* records soft-deleted at source by the nightly reconcile; programs still upcoming or in progress, by computed_status not status.
*Why it changed:* Was 'Internal %'. Same formula; the old name read as internal to the company, which is not what the flag distinguishes — `internal` means L&D delivered it, `external` means another department or a vendor did.

### Corrected

The published figure was wrong. These are the difficult conversations, and each one has a defect number attached to it.

| Metric | Workbook | Platform | n |
|---|---|---|---|
| Knowledge Relevance | 100.0% | **97.5%** | 118 |
| Activity Effectiveness | 98.2% | **97.5%** | 118 |
| Logistics Effectiveness | 96.4% | **91.5%** | 118 |
| Net Promoter Score | 92.7% | **+83.1** | 118 |
| Participation Rate | 60.4% | **9.3%** | 1,455 |


**Knowledge Relevance** — Share of responses rating this knowledge as relevant 4 or better on the 1-5 scale, over every response in scope that answered the question.

*Counts over:* survey responses carrying a rating for the dimension asked about.
*Excludes:* records soft-deleted at source by the nightly reconcile; rows whose person could not be identified — quarantined, never dropped; responses that left this question blank.
*Why it changed:* Formula unchanged. The workbook computed it over a silently filtered 55 of 77 responses; this runs over every response in the declared population (P-03).
*Workbook figure from BRD v1.5.*

**Activity Effectiveness** — Share of responses rating this activity as effective 4 or better on the 1-5 scale, over every response in scope that answered the question.

*Counts over:* survey responses carrying a rating for the dimension asked about.
*Excludes:* records soft-deleted at source by the nightly reconcile; rows whose person could not be identified — quarantined, never dropped; responses that left this question blank.
*Why it changed:* Formula unchanged. The workbook computed it over a silently filtered 55 of 77 responses; this runs over every response in the declared population (P-03).
*Workbook figure from BRD v1.5.*

**Logistics Effectiveness** — Share of responses rating this logistics as effective 4 or better on the 1-5 scale, over every response in scope that answered the question.

*Counts over:* survey responses carrying a rating for the dimension asked about.
*Excludes:* records soft-deleted at source by the nightly reconcile; rows whose person could not be identified — quarantined, never dropped; responses that left this question blank.
*Why it changed:* Formula unchanged. The workbook computed it over a silently filtered 55 of 77 responses; this runs over every response in the declared population (P-03).
*Workbook figure from BRD v1.5.*

**Net Promoter Score** — (promoters - detractors) / responses, on the standard -100 to +100 scale. Promoters score 9-10, passives 7-8, detractors 0-6.

*Counts over:* survey responses carrying a rating for the dimension asked about.
*Excludes:* records soft-deleted at source by the nightly reconcile; rows whose person could not be identified — quarantined, never dropped; responses that left this question blank.
*Why it changed:* Two changes. Scope: over every response rather than a filtered 55 of 77 (P-03). Unit: the workbook's 92.7% was a percentage; NPS is a -100..+100 index. The two are not comparable and must not be shown as if they were.
*Workbook figure from BRD v1.5 — published as a percentage; NPS is an index.*

**Participation Rate** — Distinct people who attended at least one session in the period, over active employees the CRM can enroll as at the period end.

*Counts over:* active employees the CRM can enroll, as of the period end.
*Excludes:* any fixed number — the denominator is counted, never typed; people the roster no longer returns — 149 leavers and two closed companies; employees whose status is not active.
*Why it changed:* Wrong twice in the workbook. The denominator was a hardcoded 192 (P-01), and it divided five companies' attendance by one company's headcount (P-13). Both halves now come from the same roster. Never present the old 60.4% or 66.7%.
*Workbook figure from BRD v1.5 — and 66.7% elsewhere in the same workbook.*

### New

The workbook had no equivalent. Nothing to reconcile — but each needs its definition read once before anybody acts on it.

| Metric | Workbook | Platform | n |
|---|---|---|---|
| No-show Rate | — | **36.9%** | 482 |
| Fill Rate | — | **68.4%** | 27 |
| Survey Response Rate | — | **38.8%** | 304 |
| Coverage Gap | — | **1,320** | 1,455 |
| Months Since Last Training | — | **2.3 months** | 147 |
| LinkedIn Hours | — | **—** | — |
| Blended Learner Hours | — | **—** | — |
| Unique Reach | — | **—** | — |


**No-show Rate** — Share of enrollments where the person never attended any session of the program they enrolled on.

*Counts over:* people enrolled on a program.
*Excludes:* records soft-deleted at source by the nightly reconcile; rows whose person could not be identified — quarantined, never dropped.

**Fill Rate** — Enrollments over declared capacity, across programs that state a capacity.

*Counts over:* programs the CRM marks completed.
*Excludes:* records soft-deleted at source by the nightly reconcile; programs still upcoming or in progress, by computed_status not status.
*Why it changed:* Programs with no declared capacity are excluded, not counted as zero.

**Survey Response Rate** — Survey responses over distinct people who attended.

*Counts over:* one person attending one session.
*Excludes:* records soft-deleted at source by the nightly reconcile; rows whose person could not be identified — quarantined, never dropped.
*Why it changed:* Qualifies every quality score. A high score over a low response rate is a weaker claim.

**Coverage Gap** — Active employees with no attendance in the period.

*Counts over:* active employees the CRM can enroll, as of the period end.
*Excludes:* any fixed number — the denominator is counted, never typed; people the roster no longer returns — 149 leavers and two closed companies; employees whose status is not active.
*Why it changed:* Not derivable from attendance alone — these people appear in no program payload.

**Months Since Last Training** — Median months since a person's most recent attendance, over people who have attended.

*Counts over:* one person attending one session.
*Excludes:* records soft-deleted at source by the nightly reconcile; rows whose person could not be identified — quarantined, never dropped.
*Why it changed:* Median, not mean — the tail is long and a mean would describe nobody.

**LinkedIn Hours** — Hours of LinkedIn Learning content completed, from the scheduled export.

*Counts over:* one person attending one session.
*Excludes:* records soft-deleted at source by the nightly reconcile; rows whose person could not be identified — quarantined, never dropped.
*Why it changed:* Awaiting the LinkedIn Learning export. The delivery plan puts it out of v1 for want of a feed, so this reports no value rather than a zero — there has been no measurement, which is not the same as a measurement of none.
*Not computable yet.* No value is produced for this metric on the current data — see **What is still blocked** below.

**Blended Learner Hours** — Classroom learner hours plus LinkedIn Learning hours, for one blended total.

*Counts over:* one person attending one session.
*Excludes:* records soft-deleted at source by the nightly reconcile; rows whose person could not be identified — quarantined, never dropped.
*Why it changed:* Awaiting the LinkedIn Learning export. The delivery plan puts it out of v1 for want of a feed, so this reports no value rather than a zero — there has been no measurement, which is not the same as a measurement of none.
*Not computable yet.* No value is produced for this metric on the current data — see **What is still blocked** below.

**Unique Reach** — Distinct people reached by any learning, classroom or LinkedIn, counted once each.

*Counts over:* one person attending one session.
*Excludes:* records soft-deleted at source by the nightly reconcile; rows whose person could not be identified — quarantined, never dropped.
*Why it changed:* Awaiting the LinkedIn Learning export. The delivery plan puts it out of v1 for want of a feed, so this reports no value rather than a zero — there has been no measurement, which is not the same as a measurement of none. Until then Total Participants is the classroom-only answer, and the two must not be presented as the same figure.
*Not computable yet.* No value is produced for this metric on the current data — see **What is still blocked** below.

## The same figures over the full dataset

July 2025 to September 2026. **Not comparable to the workbook column** — it covers roughly twice the period. This is what the platform will publish once it is live.

| Metric | Feb-Aug 2026 | Full dataset | n (full) |
|---|---|---|---|
| Total Participants | 147 | 287 | 1,165 |
| Learner Hours | 1,153.0 hours | 3,686.5 hours | 1,165 |
| Facilitator Performance | 98.3% | 98.7% | 297 |
| Training Days | 50 | 119 | 119 |
| Training Hours Delivered | 135.6 hours | 365.2 hours | 119 |
| Total Programs | 27 | 55 | 55 |
| L&D-delivered % | 96.3% | 89.1% | 55 |
| Public vs Customised | 81.5% | 87.3% | 55 |
| Knowledge Relevance | 97.5% | 97.6% | 297 |
| Activity Effectiveness | 97.5% | 98.0% | 297 |
| Logistics Effectiveness | 91.5% | 91.9% | 297 |
| Net Promoter Score | +83.1 | +88.2 | 297 |
| Participation Rate | 9.3% | 13.8% | 1,455 |
| No-show Rate | 36.9% | 38.7% | 1,060 |
| Fill Rate | 68.4% | 70.9% | 55 |
| Survey Response Rate | 38.8% | 45.7% | 650 |
| Coverage Gap | 1,320 | 1,254 | 1,455 |
| Months Since Last Training | 2.3 months | 5.0 months | 287 |
| LinkedIn Hours | — | — | — |
| Blended Learner Hours | — | — | — |
| Unique Reach | — | — | — |

## What is still blocked

These metrics produce no value on the current data:

- **Blended Learner Hours**
- **LinkedIn Hours**
- **Unique Reach**

The five quality metrics and NPS are blocked on one thing: `app.survey_question_map` is empty, so no answer can be attributed to a measured dimension. All survey responses are stored; none are scored. The CRM runs a single shared survey whose questions are stable across every program — q3 knowledge, q4 activity, q5 logistics, q6 facilitator, q7 recommend — so the map is five rows, and until it exists the stakeholder gate cannot be held on the figures L&D most wants to see.

LinkedIn Hours, Blended Learner Hours and Unique Reach have no source connected. They are scheduled for weeks 7-8 and are first to be cut.

## The conversations that need care

### Participation Rate: the largest restatement in the project

It was wrong twice over. The denominator was the literal number 192 typed into a formula (P-01), and it then divided five companies' attendance by one company's headcount (P-13). Both halves now come from the same roster.

Over the workbook's window it reads **9.3%** — 135 people who attended, over 1,455 active employees the CRM can enroll.

The old 60.4% and 66.7% are not larger versions of this number. They are a different measurement, and presenting them side by side invites the reading that participation collapsed. It did not: it was never measured.

### NPS: two changes, and only one is a correction

The scope was wrong — the published figure ran over a silently filtered 55 of 77 responses (P-03). But the **unit** was also wrong: 92.7% is a percentage, and NPS is an index from -100 to +100.

Shown without explanation, an NPS of +88 reads as a fall from 92.7. It is not, and nothing about satisfaction changed. Say: *NPS is now reported on the standard -100 to +100 scale.* Then give the number.

### Learner Hours is lower than the workbook, and that needs an answer

Over the same window the platform reports **1,153.0 hours** against the workbook's 1,386.0. Every other figure moved upward, so this one will be asked about.

It is the only figure here where the workbook is *higher* than the platform, and it is not explained by coverage. Learner Hours is session duration summed once per attendance, so the likely cause is the workbook's single `hours` column being read as both delivered hours and learner hours in different places. Confirm the cause before the meeting rather than in it.

## How to present this

Bring the evidence, not the summary: the hardcoded 192, the two `Hard Talks` program ids, the two spellings of one trainer's name, and the pivot filter that drops the two largest programs from every quality score. Let the file make the argument.

Lead with coverage, because it explains most of the movement and nobody disputes it. Then the restated figures. Then the corrections, each with its defect number. Save Participation Rate and NPS for last and give them room — see the notes on those two above.

**Never present the workbook's 60.4% or 66.7% as comparable to the current participation rate.** They are not smaller versions of the same measurement; they are a different measurement.

## Sign-off

| | |
|---|---|
| Every restated figure walked through with L&D | ☐ |
| Every corrected figure accepted, with its cause | ☐ |
| Blocked metrics acknowledged as outstanding | ☐ |
| L&D Director signature | |
| Date | |
